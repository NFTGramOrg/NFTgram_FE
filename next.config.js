/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "randompokemon.com",
      "avatars.githubusercontent.com",
      "cdn.neoline.io",
    ],
  },
};

module.exports = nextConfig;
